# 跨页面布局对齐

Review date: 2026-09-14

## 范围

按用户要求统一各界面的 UI 位置，保留现有配色、内容与业务行为。
项目入口依据工作区 Wiki `wiki/50-features/long-term-projects-map.md`；
验证流程依据 `wiki/60-codex/workflow/ai-development-delivery.md`。
本轮不提交、不部署，不修改样本记录。

## 修改

- 新增最后加载的 `src/layout.css`，统一顶部导航、搜索、工作区边距及栏宽。
- 主表与回检共用标题、三项工具操作、四项统计、筛选区和表格起点。
- 批量复核移至回检工具栏；切换主表和回检不再强制展开详情侧栏。
- 首页移除固定最小宽度，中等窗口两列、小屏单列，并保留区域内滚动。
- 修正小屏首页内容压缩重叠；对齐小屏统计、筛选和表格左右边距。
- 统一导出和字段模板弹窗边距、标题及关闭按钮位置。
- 隐藏首页未实现的禁用日志筛选，未移除已有有效筛选功能。

## 验证

- 截图检查首页、主表、回检、导出和设置；覆盖 390×844、1024×768、1280×800，另检查 1440×900 首页和设置。
- 1280×800 下，主表与回检的标题、统计、筛选区、表格、右栏 DOM 边界逐项相同；表格顶部均为 502px，右栏左边界均约 891.20px。
- 1024×768 下，两页上述区域也逐项一致；表格顶部均为 542px，右栏左边界均为 692px。
- 390×844 下核对主表与回检共同区域位置及弹窗视口边界；小屏首页改为顺序布局，区域不再重叠。
- 最终返回 `http://127.0.0.1:8765/index.html#detail`，视口 1280×800，无页面横向溢出，浏览器错误日志为空。
- 原工作区仍为 409 条，已确认 0 条、已修改 0 条；本轮未执行记录保存或批量修改。
- `npm test`：65 项 JavaScript 和 3 项 Python 测试全部通过，共 68 项。
- `npm run check`、`git diff --check` 通过。云端检查仅为静态结构检查。

## 边界

不同页面仍保留适合任务的内容和局部强调，不要求所有文本高度完全相同。
本轮是布局对齐，不是全部业务流程或云端部署验收；未重新测试导入预览的完整操作链路。
